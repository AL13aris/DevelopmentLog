---
date: 2026-06-05
tags:
  - devlog
  - til
---


# 개발 일지 & TIL

> [!info]- 작성 가이드 (클릭해서 펼치기)
> **[개발 일지 가이드]**
> - 오늘 구현한 기능 / 수정한 버그 / 막혔던 문제 및 해결 과정
> - 참고한 문서나 자료 / 내일 할 작업
> - 현재 프로젝트의 위험 요소 / 나중에 리팩토링 할 부분 / 기능을 구현하며 배운 점
> 
> **[TIL 가이드]**
> - 오늘 처음 이해한 문법 이나 개념 (C++, UE5 등)
> - 오류 메시지 분석 / API 사용법 / 자료구조 및 디자인 패턴 적용 이유
> - 공식 문서 내용 / 성능 문제를 확인한 방법

## 기본 정보
- **날짜:** 2026-06-05
- **프로젝트:** 이득우의 언리얼 엔진 프레임 워크

## 오늘 한 작업
-  언리얼 오브젝트 리플렉션 시스템 2
-  언리얼 에서 클래스 상속 과정 다룸
-  언리얼 엔진 리플렉션 시스템

## 발생한 문제 & 해결 방법
### 문제 상황
-  

### 해결 방법
- 

### 배운 점
- 언리얼 헤더 상속시 주의 .generated.h 아래 에는 절대 하지 말 것
- 상속을 수동으로 하는 법 // 자동은 언리얼 엔진 내부에서
- 

```cpp
class OBJECTREFLECTION_API UStudent : public UPerson <- 여길 상속 받을 클래스 이름으로 바꿔주면됨
```

- 언리얼 엔진 오브젝트 생성
```cpp
UStudent* Student = new UStudnet           // X new로 생성 안함 
UStudent* Student = NewObject<UStudent>(); // O
```

- 리플렉션 시스템으로 언리얼 오브젝트의 특정 속성(프로퍼티), 함수 를 이름으로 검색이 가능하다
- 리플렉션 시스템을 사용해 접근 지시자와 무관하게 값을 호출 가능하다
- 리플렉션 시스템을 사용해 함수 도 호출이 가능하다
### 다음 작업
- 



# 오늘 배운 점 (TIL)
### 알게 된 내용: 
- 언리얼 헤더 상속시 주의 .generated.h 아래 에는 절대 하지 말 것
- 리플렉션 시스템으로 언리얼 오브젝트의 특정 속성(프로퍼티), 함수 를 이름으로 검색이 가능하다
- 리플렉션 시스템을 사용해 접근 지시자와 무관하게 값을 호출 가능하다
- 리플렉션 시스템을 사용해 함수 도 호출이 가능하다


즉 리플렉션 시스템은

엔진 내부에 언리얼 매크로를 통해 등록된 변수, 함수 등등을 보관하고 있다가
이름으로 요청을 하면 엔진에서 찾아서 사용을 한다
매크로로 엔진에 등록을 해야 엔진이 찾아서 사용을 할 수 있다

### 헷갈렸던 점
- 
- 


### 예제 코드 또는 상황
## CPP
```cpp
// 필요한 경우 코드 작성
UStudent* Student = NewObject<UStudent>();  
UTeacher* Teacher = NewObject<UTeacher>();  
  
Student->SetName(TEXT("학생 1"));  
UE_LOG(LogTemp, Log , TEXT("새로운 학생 이름 %s "), *Student->GetName());  
  
//리플렉션 시스템을 이용 해서 이름 변경 하기  
FString CurrentTeacherName;  
FString NewTeacherName = TEXT("이득우");  
FProperty* NameProp = UTeacher::StaticClass()->FindPropertyByName(TEXT("Name"));  
if (NameProp)  
{  
    NameProp->GetValue_InContainer(Teacher, &CurrentTeacherName);  
    UE_LOG(LogTemp, Log , TEXT("현재 선생님 이름 %s "), *Teacher->GetName());  
      
    NameProp->SetValue_InContainer(Teacher, &NewTeacherName);  
    UE_LOG(LogTemp, Log , TEXT("새로운 선생님 이름 %s "), *Teacher->GetName());  
}  
UE_LOG(LogTemp, Log, TEXT("==========================="));  
  
Student->DoLesson();  
  
UFunction* DoLessonFuc = 
Teacher->GetClass()->FindFunctionByName(TEXT("DoLesson"));  
if (DoLessonFuc)  
{  
    Teacher->ProcessEvent(DoLessonFuc, nullptr);  
}

```
## 중요 함수들
```cpp
// 필요한 경우 코드 작성
FProperty
GetClass()// <- 객체가 생성된후 그 객체가 어떤 클래스 인지 확인
StaticClass()//<- 클래스 정보를 가져옴
//엔진 내부에서 조회 하는 함수 U 메크로로 통해 등록된 변수 함수 를 찾는 함수
//`FName`은 코드의 변수 이름과 정확히 일치해야 합니다.
FindPropertyByName(FName Name)
FindFunctionByName(FName Name)
//In_Container <- 메모리 주소 접근하여 값 조작
GetValue_InContainer(void* Container, void* OutValue)
SetValue_InContainer(void* Container, void* NewValue)
ProcessEvent(UFunction* Function, void* Parms)//<- 리플렉션 시스템을 통해 즉시 실행
```



### 다음에 더 공부 할 것
- 언리얼 엔진 리플렉션 시스템 응용
- 인터페이스

## 참고 자료

언리얼 엔진 리플렉션 시스템
https://dev.epicgames.com/documentation/unreal-engine/reflection-system-in-unreal-engine?application_version=5.5