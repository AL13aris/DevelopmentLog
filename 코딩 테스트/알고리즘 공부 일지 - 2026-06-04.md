---
date: 2026-06-04
tags:
  - devlog
  - til
  - CodingTest
---


# 개발 일지 & TIL

> [!info]- 작성 가이드 (클릭해서 펼치기)
> **[개발 일지 가이드]**
> - 오늘 구현한 기능 / 수정한 버그 / 막혔던 문제 및 해결 과정
> - 참고한 문서나 자료 / 내일 할 작업
> - 현재 프로젝트의 위험 요소 / 나중에 리팩토링 할 부분 / 기능을 구현하며 배운 점
> 
> **[TIL 가이드]**
> - 오늘 처음 이해한 문법 이나 개념 (C++, UE5 등)
> - 오류 메시지 분석 / API 사용법 / 자료구조 및 디자인 패턴 적용 이유
> - 공식 문서 내용 / 성능 문제를 확인한 방법

## 기본 정보
- **날짜:** 2026-06-04
- **프로젝트:** 기초 프르래밍 3일차

## 오늘 한 작업
-  [1]문자열 섞기
-  [2]문자 리스트 문자열 변환
-  [3]더 크게 합치기( 문자열로 합친 후 정수 비교)

## 발생한 문제 & 해결 방법
### 문제 상황
- [1]Vector string 으로 넣을려고 했지만 안들어감 string 으로 받아와 str[i] 로 호출시 이 값은 char 값이라 Vector string 에 안들어가는 문제
- [3]문서를 보고 stoi를 활용 하는것을 알았지만 어떻게 쓰는지 모르는 상태

### 해결 방법
- [1]vector char로 하고 값을 넣은 뒤 string 변환
- [3]string 변수에 to_string으로 변환하여 문자열로 집어 넣고 stoi(string 변수)로 정수 변환해서 비교

### 배운 점
- [1]string 형식이어도 내부 값은 char 이다
- [1]string 으로 변환 할 때는 basic_string 에 여러가지 생성자를 이용해서 변환 한다
- [3]string 변환을 좀 더 공부 해야 할 것

### 다음 작업
- 



# 오늘 배운 점 (TIL)
### 알게 된 내용: 
- [1]basic_string 에는 여러가지 생성자가 있다 그걸 잘 활용 하자
- stoi 사용법
- max 함수로 간단하게 줄일 수 있다

### 헷갈렸던 점
- [1]str[i] 로 가져올 때는 char 값 이라는 걸 헷갈렸다
- [3]string 변환을 하는걸 몰랐다


### 예제 코드 또는 상황
## CPP
```cpp
// 필요한 경우 코드 작성
string solution(string str1, string str2) {
    
   
    vector<char> answerV;
    for(int i = 0; i <str1.length(); i++)
    {
        answerV.push_back(str1[i]);
        answerV.push_back(str2[i]);
    }
    string answer(answerV.begin(), answerV.end());
    return answer;
}
```

## CPP max 쓰기전
```cpp
solution(int a, int b) {
    int answer = 0;
    string S1 = to_string(a) + to_string(b);
    string S2 = to_string(b) + to_string(a);

    if(stoi(S1) == stoi(S2))
    {
        answer = stoi(S1);
    }
    else if(stoi(S1) < stoi(S2))
    {
        answer = stoi(S2);
    }
    else if(stoi(S1) > stoi(S2))
    {
        answer = stoi(S1);
    }
```
## CPP max 쓰기
```cpp
int solution(int a, int b) {
    return max(stoi(to_string(a)+to_string(b)),stoi(to_string(b)+to_string(a)));
}
```
## .h
```.h
// 필요한 경우 코드 작성

```


### 다음에 더 공부 할 것
- 
- 

## 참고 자료

- 마이크로 소프트 String 클래스 중 basic_string
  ## `basic_string::basic_string`

비어 있거나 특정 문자로 초기화된 문자열 또는 다른 문자열 개체나 C 스타일(null 종료) 문자열 일부 또는 전체의 복사본인 문자열을 생성합니다.
https://learn.microsoft.com/ko-kr/cpp/standard-library/basic-string-class?view=msvc-170#basic_string
- vector 
  https://learn.microsoft.com/ko-kr/cpp/standard-library/vector-class?view=msvc-170
- string 함수
  https://learn.microsoft.com/ko-kr/cpp/standard-library/string-functions?view=msvc-170
- 